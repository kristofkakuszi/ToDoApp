package com.example.todo.data.models

enum class Priority {   //Enum is a group of constants (unchangeable variables, like final variables)
    //egyik oszlop a 4 kozul
    HIGH,
    MEDIUM,
    LOW
}